export enum RoshamboOption {
  rock,
  paper,
  scissors,
}

export enum PayType {
  cash,
  coin,
}

export enum choices {
  at_stated_timed,
  immediately,
}

export enum notificationHintType {
  withdraw, // orange
  fund, // blue
  lost,// red
  win, // green
  draw, // yellow
}